package com.ss.config;

public enum Type {
  blank, grape, banana, kiwi, orange, apple,
  strawberry, lemon, raspberry, blueberry, clock, walnut, glass_fruit, jam
}

/*
khi thêm item mới vào => thêm case tại util.getRegion() và util.getType(), sửa số lượng item (AMOUNT_ITEM) trong file
config
 */
